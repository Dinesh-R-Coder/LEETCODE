class Solution {
    public boolean rotateString(String s, String goal) {
        String check = s + s ;  // Add the string 2 times to check the availability of the string
        

        if(s.length() != goal.length()){
            return false ;
        }
        if(check.contains(goal)){
            return true ;
        }
        return false ;
    }
}
